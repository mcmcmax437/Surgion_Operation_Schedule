# Local only (optional). VPS_* and ACCESS_PASSWORD for CI live in
# GitHub → Settings → Secrets and variables → Actions

ACCESS_PASSWORD=

# GitHub secret VPS_DEPLOY_PATH should be:
# /usr/src/surgion_operation/Surgion_Operation_Schedule
